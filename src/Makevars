#################################################
# Compilers, GCC is used to create dependencies
#################################################

PKG_CPPFLAGS =  -I"./include" -DUSE_DOUBLE

MAIN         = $(wildcard *.cpp)
OBJECTS      = $(MAIN:.cpp=.o)
